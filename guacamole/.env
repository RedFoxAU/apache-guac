# Apache Guacamole Docker Compose Environment Configuration
# ============================================================
# 
# This file contains environment variables for the Guacamole Docker Compose setup.
# Copy this file and customize the values for your environment.
#
# SECURITY NOTE: This file contains sensitive information. 
# Ensure proper file permissions (600) and never commit passwords to git.

# =============================================================================
# WORKING DIRECTORY CONFIGURATION
# =============================================================================
# Base directory for all Docker bind mounts
# Default: /home/$USER/docker (replace $USER with your actual username)
WORKING_DIR=/home/$USER/docker

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# PostgreSQL Database Settings
POSTGRES_DB=guacamole_db
POSTGRES_USER=guacamole_user
# NOTE: Password is stored in secrets file for security

# =============================================================================
# GUACAMOLE WEB APPLICATION CONFIGURATION
# =============================================================================
# Port for Guacamole web interface
GUACAMOLE_PORT=8080

# Web application context path (usually ROOT for main path)
WEBAPP_CONTEXT=ROOT

# Guacamole daemon log level (trace, debug, info, warn, error)
GUACD_LOG_LEVEL=info

# =============================================================================
# NETWORK AND PROXY CONFIGURATION
# =============================================================================
# Timezone setting
TZ=UTC

# Enable remote IP valve for proxy setups
REMOTE_IP_VALVE_ENABLED=false

# Regex for allowed proxy IPs (if using reverse proxy)
PROXY_ALLOWED_IPS_REGEX=

# =============================================================================
# NGINX REVERSE PROXY CONFIGURATION (Optional)
# =============================================================================
# Nginx HTTP and HTTPS ports
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# =============================================================================
# LDAP AUTHENTICATION (Optional - uncomment and configure if using LDAP)
# =============================================================================
# LDAP_HOSTNAME=ldap.example.com
# LDAP_PORT=389
# LDAP_USER_BASE_DN=ou=users,dc=example,dc=com
# LDAP_USERNAME_ATTRIBUTE=uid
# LDAP_SEARCH_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_SEARCH_BIND_PASSWORD=ldap_password

# =============================================================================
# ADDITIONAL GUACAMOLE EXTENSIONS (Uncomment as needed)
# =============================================================================
# GUACAMOLE_ADMIN_USERNAME=guacadmin
# GUACAMOLE_ADMIN_PASSWORD=guacadmin
# ENABLE_TOTP=false
# TOTP_ISSUER=Apache Guacamole 
