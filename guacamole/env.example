# .env
# Secrets and configuration for Guacamole Docker Compose setup

# --- Database Settings ---
POSTGRES_DB=guacamole_db
POSTGRES_USER=guacamole_user
POSTGRES_PASSWORD=your_secure_password

# --- Guacamole Web Application Settings ---
# GUACAMOLE_HOME=/config
# GUACD_HOSTNAME=guacd
# GUACAMOLE_LOG_LEVEL=INFO

# --- Guacamole Database Connection (Optional, override if needed) ---
# POSTGRES_HOSTNAME=postgres
# POSTGRES_PORT=5432

# --- Guacamole Default Admin User (for LDAP/other non-db auth setups) ---
# GUACAMOLE_ADMIN_USERNAME=guacadmin
# GUACAMOLE_ADMIN_PASSWORD=guacadmin

# --- Server Context (usually don't need to change) ---
WEBAPP_CONTEXT=ROOT

# --- Timezone Settings ---
# TZ=Australia/Perth

# --- Proxy/Networking (uncomment if needed) ---
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=https://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1

# --- LDAP Authentication (if using LDAP, uncomment and configure) ---
# LDAP_HOSTNAME=ldap.example.com
# LDAP_PORT=389
# LDAP_USER_BASE_DN=ou=users,dc=example,dc=com
# LDAP_USERNAME_ATTRIBUTE=uid
# LDAP_SEARCH_BIND_DN=cn=admin,dc=example,dc=com
# LDAP_SEARCH_BIND_PASSWORD=ldap_password%                                      audino@deb ~/docker/guacamole % 
